{
  "name": "@yoshibet/slots-engine",
  "version": "0.1.0",
  "description": "Engine de slots reusável: GameConfig declarativo, RNG provably fair, resolvedor de paylines e simulador de RTP. Zero dependências de UI.",
  "private": true,
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/test/*.test.js",
    "simulate": "tsc && node dist/scripts/simulate.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}