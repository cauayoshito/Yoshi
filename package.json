{
  "name": "yoshibet",
  "version": "1.0.0",
  "private": true,
  "description": "YOSHI BET — plataforma iGaming demo (front estático + API em server/)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm ci --omit=dev --prefix server",
    "start": "node server/src/index.js",
    "test": "npm test --prefix server",
    "seed": "npm run seed --prefix server"
  }
}
